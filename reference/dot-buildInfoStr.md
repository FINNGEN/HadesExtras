# Converts build information to a string representation.

Converts build information to a string representation.

## Usage

``` r
.buildInfoStr(buildInfo)
```

## Arguments

- buildInfo:

  Build information containing logs.

## Value

A string representation of build information.
