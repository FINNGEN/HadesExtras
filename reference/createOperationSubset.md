# A definition of subset functions to be applied to a set of cohorts

A definition of subset functions to be applied to a set of cohorts

## Usage

``` r
createOperationSubset(name = NULL, operationString)
```

## Arguments

- name:

  optional name of operator

- operationString:

  string - the operation string to be used in the query

## Value

a OperationSubsetOperator instance
