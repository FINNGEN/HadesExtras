# Converts build information to a tooltip string representation.

Converts build information to a tooltip string representation.

## Usage

``` r
.buildInfoToTooltipStr(buildInfo)
```

## Arguments

- buildInfo:

  Build information containing logs.

## Value

A tooltip string representation of build information.
